#ifndef COMMON_DENOMINATORS_H
#define COMMON_DENOMINATORS_H

#include <string>
#include <vector>

namespace Fracts {
std::string
convertFrac(const std::vector<std::vector<unsigned long long>> &rationals) {
  (void)rationals;
  return "(1,2)";
}
} // namespace Fracts

#endif
