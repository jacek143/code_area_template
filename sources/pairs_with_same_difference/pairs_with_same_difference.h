#ifndef PAIRS_WITH_SAME_DIFFERENCE_H
#define PAIRS_WITH_SAME_DIFFERENCE_H

#include <utility>
#include <vector>

namespace pairs_with_same_difference {
std::vector<std::pair<int, int>> get_pairs_with_same_diff(std::vector<int> arr,
                                                          int k);
}

#endif
